import { TouchableOpacity, TouchableOpacityProps } from "react-native";

export default function SecondaryBtn({style, ...Props } : TouchableOpacityProps) {
    return (
        <TouchableOpacity>

        </TouchableOpacity>
    )
}